pipeline{
    agent {label 'slave2'}
    stages{
        stage('Build'){
            steps{
                echo " this is jenkinsfile"
                echo 'Building...'
                sh '''
                   pwd
                   ls -l
                '''
            }
        }
        stage('Test'){
            steps{
                echo 'Testing...'
            }
        }
        stage('Deploy'){
            steps{
                echo 'Deploying...'
            }
        }
    }
}